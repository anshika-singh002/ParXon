<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParXonDemo1"
        tools:targetApi="31">

        <!-- Splash Screen -->
        <activity
            android:name=".SplashScreenOk"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Role Activity -->
        <activity android:name=".RoleActivity" />

        <!-- Rating Page Activity -->
        <activity
            android:name=".RatingPageActivity"
            android:label="Rate Difficulty"
            android:theme="@style/Theme.ParXonDemo1" />

        <!-- Remainders Activity -->
        <activity android:name=".RemaindersActivity" />

        <!-- History Activity -->
        <activity android:name=".HistoryActivity" />

        <!-- Link Activity -->
        <activity android:name=".LinkActivity" />

        <!-- Missed Items Activity -->
        <activity android:name=".MissedItemsActivity" />

        <!-- Details Activity -->
        <activity android:name=".DetailsActivity" />

        <!-- Voice Commands Activity -->
        <activity android:name=".VoiceCommandsActivity" />

        <!-- Start Exercise Activities -->
        <activity android:name=".StartExerciseActivity" />
        <activity android:name=".StartExerciseActivity2" />
        <activity android:name=".StartExerciseActivity3" />
        <activity android:name=".StartExerciseActivity4" />
        <activity android:name=".StartExerciseActivity5" />
        <activity android:name=".StartExerciseActivity6" />
        <activity android:name=".StartExerciseActivity7" />
        <activity android:name=".StartExerciseActivity8" />
        <activity android:name=".StartExerciseActivity9" />
        <activity android:name=".StartExerciseActivity10" />
        <activity android:name=".StartExerciseActivity11" />

    </application>
</manifest>
